apiVersion: v1

kind: PersistentVolumeClaim

metadata: 
     name: pvc
     namespace: bookstore

spec:
    accessModes:
      - ReadWriteOnce

    resources:
        requests:
           storage: 500Mi

