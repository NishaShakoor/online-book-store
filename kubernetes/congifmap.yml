apiVersion: v1

kind: ConfigMap

metadata:
     name: bookstore-configmap
     namespace: bookstore
data:
       DB_HOST: mysql-service
       DB_USE: root
       DB_NAME: bookstore
    
                