apiVersion: v1

kind: PersistentVolume

metadata: 
   name: pv
   namespace: bookstore

spec:
  capacity:
    storage: 1Gi

  accessModes:
    - ReadWriteOnce    

  hostPath:
    path: "/data/notes"
    